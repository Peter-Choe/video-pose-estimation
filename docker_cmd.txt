

 # container monitoting
 # docker exec -it ray_triton_fastapi_1 bash
 # docker logs -f ray_triton_fastapi_1
 # docker logs -f ray_triton_celery_worker_1
 # docker logs -f ray_triton_triton_1
 # docker exec ray_triton_fastapi_1 env | grep ENV


# docker-compose -f docker-compose.local.yml up   #triton server, redis image up 
#cd ~/ray_triton/fastapi_pipeline
# uvicorn app.main:app --reload  --port 5000  #local fastapi server up
#pkill -9 -f uvicorn #clean up fastapi server
#pkill -f "uvicorn.*--reload"

#cd ~/ray_triton/fastapi_pipeline
# celery -A app.tasks worker --loglevel=info   #local celery worker up

#pkill -9 -f 'celery' #clean up celery worker